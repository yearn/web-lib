---
title: 'Card.Tabs'
---

import {CodeExample} from '../../../components/CodeExample';
import {Card} from '@yearn-finance/web-lib/components/Card';


# Card.Tabs
```ts
import {Card} from '@yearn-finance/web-lib/components';
```

<CodeExample>
	<div style={{width: '75%'}}>
		<Card.Tabs
			tabs={[
				{label: 'Details', children: <b>{'I am the first card.'}</b>},
				{label: 'Reports', children: <p>{'I am the second Tab'}</p>},
				{label: 'Health Check', children: <p>{'And a third one?'}</p>}
			]}
		/>
	</div>
</CodeExample>

The Card.Tabs is a variant of Card that will display the Card with some available Tabs.  

## Usage

```tsx
import {Card} from '@yearn-finance/web-lib/components';

function App() {
	return (
		<Card.Tabs
			tabs={[
				{label: 'Details', children: <b>{'I am the first card.'}</b>},
				{label: 'Reports', children: <p>{'I am the second Tab'}</p>},
				{label: 'Check', children: <p>{'And a third one?'}</p>}
			]}
		/>
	);
}
```

## Properties

```tsx
type TCardTabs = {
	tabs: [
		label: string,
		children: React.ReactElement
	]
}
```


### `tabs`
Array of object containing a label, aka the title of the tab, and a children for each tab. You can have as many tab as you want, until the UI breaks (recommanded with 2/3 tabs max)
```tsx {6-10}
import {Card} from '@yearn-finance/web-lib/components';

function App() {
	return (
		<Card.Tabs
			tabs={[
				{label: 'Details', children: <b>{'I am the first card.'}</b>},
				{label: 'Reports', children: <p>{'I am the second Tab'}</p>},
				{label: 'Check', children: <p>{'And a third one?'}</p>}
			]}
		/>
	);
}
```
