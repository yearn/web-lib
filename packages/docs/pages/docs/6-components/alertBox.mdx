---
title: 'AlertBox'
---

import {CodeExample} from '../../../components/CodeExample';
import {Card, AlertBox} from '@majorfi/web-lib/components';


# AlertBox
```ts
import {AlertBox} from '@majorfi/web-lib/components';
```

<CodeExample>
	<div style={{width: '75%'}}>
		<AlertBox
			level={'warning'}
			alerts={[
				'This is my alert number 1',
				'And this is the second one',
				'Yes, I am bad at writing dummy texts'
			]} />
	</div>
</CodeExample>

The Alert Box component is used to display a group of alerts with the icon matching the alert level aside. This should be used to display, for example, a list of warnings related to a specific situation. See usage on Yearn Watch.

## Usage
```tsx
import React from 'react';
import {AlertBox} from '@majorfi/web-lib/components';

function App(): React.ReactElement {
	return (
		<AlertBox
			level={'warning'}
			alerts={[
				'This is my alert number 1',
				'And this is the second one',
				'Yes, I am bad at writing dummy texts'
			]} />
	);
}
```

## Properties
```tsx
type TAlertBox = {
	alerts: string[],
	level: 'none' | 'info' | 'warning' | 'error' | 'critical'
}
```

### `level`
Indicate the type of box to display, aka mostly the colors. Default is set to info.

<CodeExample>
	<div className="w-3/4 flex flex-col space-y-2">
		<AlertBox
			level={'info'}
			alerts={[
				'This is my alert number 1',
				'And this is the second one',
				'Yes, I am bad at writing dummy texts'
			]} />
		<AlertBox
			level={'warning'}
			alerts={[
				'This is my alert number 1',
				'And this is the second one',
				'Yes, I am bad at writing dummy texts'
			]} />
		<AlertBox
			level={'error'}
			alerts={[
				'This is my alert number 1',
				'And this is the second one',
				'Yes, I am bad at writing dummy texts'
			]} />
		<AlertBox
			level={'critical'}
			alerts={[
				'This is my alert number 1',
				'And this is the second one',
				'Yes, I am bad at writing dummy texts'
			]} />
	</div>
</CodeExample>

```tsx {7}
import React from 'react';
import {AlertBox} from '@majorfi/web-lib/components';

function App(): React.ReactElement {
	return (
		<AlertBox
			level={'critical'}
			alerts={[
				'This is my alert number 1',
				'And this is the second one',
				'Yes, I am bad at writing dummy texts'
			]} />
	);
}
```

### `alerts`
List of message to display.

```tsx {8-12}
import React from 'react';
import {AlertBox} from '@majorfi/web-lib/components';

function App(): React.ReactElement {
	return (
		<AlertBox
			level={'critical'}
			alerts={[
				'This is my alert number 1',
				'And this is the second one',
				'Yes, I am bad at writing dummy texts'
			]} />
	);
}
```
