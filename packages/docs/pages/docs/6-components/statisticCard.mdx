---
title: 'StatisticCard'
---

import {CodeExample} from '../../../components/CodeExample';
import {Card} from '@yearn-finance/web-lib/components/Card';
import {StatisticCard} from '@yearn-finance/web-lib/components/StatisticCard';


# StatisticCard
```ts
import {StatisticCard} from '@yearn-finance/web-lib/components';
```
<CodeExample>
	<div className="w-3/4">
		<StatisticCard.Wrapper>
			<StatisticCard
				label={'TVL'}
				value={'1 003 030 $'} />
			<StatisticCard
				label={'Audit Score'}
				value={'2'} />
			<StatisticCard
				label={'Complexity Ratio'}
				value={'42%'} />
		</StatisticCard.Wrapper>
	</div>
</CodeExample>

The StatisticCard is a simple component used to display, in a Card, a label and a value. This is mostly used to highlight some important numbers or informations.  
A subcomponent, the StatisticCard.Wrapper, is also available in order to wrap a bunch of StatisticCard together. Size of each card and grid should be controlled with the className props. The wrapper is just a div with `grid grid-cols-12 gap-4` as className.  
This component extends the Card component.

## Usage

```tsx
import React from 'react';
import {StatisticCard} from '@yearn-finance/web-lib/components';

function	App(): React.ReactElement {
	return (
		<StatisticCard.Wrapper>
			<StatisticCard
				label={'TVL'}
				value={'1 003 030 $'} />
			<StatisticCard
				label={'Audit Score'}
				value={'2'} />
			<StatisticCard
				label={'Complexity Ratio'}
				value={'42%'} />
		</StatisticCard.Wrapper>
	);
}
```

## Properties

```tsx
type TStatisticCard = {
	label: string;
	value: ReactNode;
	variant?: 'surface' | 'background';
} & React.ComponentPropsWithoutRef<'div'>;
```

### `label`
Small displayed text to indicate what the statistic is about.
```tsx {8}
import React from 'react';
import {StatisticCard} from '@yearn-finance/web-lib/components';

function App(): React.ReactElement {
	return (
		<StatisticCard.Wrapper>
			<StatisticCard
				label={'TVL'}
				value={'1 003 030 $'} />
		</StatisticCard.Wrapper>
	);
}
```

### `value`
Statistic to display with a bolder and bigger font.
```tsx {9}
import React from 'react';
import {StatisticCard} from '@yearn-finance/web-lib/components';

function App(): React.ReactElement {
	return (
		<StatisticCard.Wrapper>
			<StatisticCard
				label={'TVL'}
				value={'1 003 030 $'} />
		</StatisticCard.Wrapper>
	);
}
```

### `className` (optional)
Custom className to provide to alter the style of the Card. This could be useful to define the size of the card in the grid. Default is `col-span-12 md:col-span-4` with a `grid-cols-12` wrapper.

<CodeExample>
	<div className="w-3/4">
		<StatisticCard.Wrapper>
			<StatisticCard
				className={'col-span-12'}
				label={'TVL'}
				value={'1 003 030 $'} />
		</StatisticCard.Wrapper>
	</div>
</CodeExample>

```tsx {8}
import React from 'react';
import {StatisticCard} from '@yearn-finance/web-lib/components';

function App(): React.ReactElement {
	return (
		<StatisticCard.Wrapper>
			<StatisticCard
				className={'col-span-12'}
				label={'TVL'}
				value={'1 003 030 $'} />
		</StatisticCard.Wrapper>
	);
}
```


### `variant` (optional)
Used to indicate if the Card is displayed on the Background (use surface) or on a Surface (use background). Default set to surface.

<CodeExample>
	<div className="w-3/4">
		<Card>
			<StatisticCard.Wrapper>
				<StatisticCard
					variant={'background'}
					label={'TVL'}
					value={'1 003 030 $'} />
			</StatisticCard.Wrapper>
		</Card>
	</div>
</CodeExample>

```tsx {9}
import React from 'react';
import {Card, StatisticCard} from '@yearn-finance/web-lib/components';

function App(): React.ReactElement {
	return (
		<StatisticCard.Wrapper>
			<Card>
				<StatisticCard
					variant={'background'}
					label={'TVL'}
					value={'1 003 030 $'} />
			</Card>
		</StatisticCard.Wrapper>
	);
}
```
